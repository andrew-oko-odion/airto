<%= form_with(model: event_space, local: true) do |form| %>
  <% if event_space.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event_space.errors.count, "error") %> prohibited this event_space from being saved:</h2>

      <ul>
      <% event_space.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :location %>
    <%= form.text_field :location, id: :event_space_location %>
  </div>

  <div class="field">
    <%= form.label :rate %>
    <%= form.text_field :rate, id: :event_space_rate %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :event_space_description %>
  </div>

  <div class="field">
    <%= form.label :amenity_id %>
    <%= form.text_field :amenity_id, id: :event_space_amenity_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :event_space_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
